import { ExtraValuePipe } from './extra-value.pipe';

describe('ExtraValuePipe', () => {
  it('create an instance', () => {
    const pipe = new ExtraValuePipe();
    expect(pipe).toBeTruthy();
  });
});
