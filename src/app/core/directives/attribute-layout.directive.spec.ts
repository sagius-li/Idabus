import { AttributeLayoutDirective } from './attribute-layout.directive';

describe('AttributeLayoutDirective', () => {
  it('should create an instance', () => {
    const directive = new AttributeLayoutDirective();
    expect(directive).toBeTruthy();
  });
});
