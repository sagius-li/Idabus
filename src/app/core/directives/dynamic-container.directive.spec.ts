import { DynamicContainerDirective } from './dynamic-container.directive';

describe('DynamicContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicContainerDirective();
    expect(directive).toBeTruthy();
  });
});
